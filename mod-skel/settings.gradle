pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.fabricmc.net/' }
        maven { url = 'https://maven.aliyun.com/nexus/content/groups/public/' } // fallback
    }
}

rootProject.name = 'transcriber-plugin'